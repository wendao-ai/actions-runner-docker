# 敏感凭据（绝对不能提交）
.env
.env.*
!.env.example
# runner 注册凭据（含 RSA 私钥），per-host
.creds/
# per-host bind-mount 覆盖（指向 .creds/）
docker-compose.override.yml

# 构建产物 / 临时文件
build.log
*.log
*.tmp

# 编辑器
.vscode/
.idea/
*.swp
.DS_Store

# OS
Thumbs.db
